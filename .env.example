# Copy this file to .env.local and fill in the values below.
# .env.local is gitignored and never ships to the browser bundle except via
# Vite's import.meta.env (anon key is safe; RLS protects all data).
#
# For Vercel deployment: add these same vars in the Vercel project settings.

VITE_SUPABASE_URL=https://fpnlcwxmyodpvmmpxksg.supabase.co
VITE_SUPABASE_ANON_KEY=PASTE-ANON-KEY-FROM-SUPABASE-DASHBOARD
